{
  "name": "url-shortener",
  "version": "1.0.0",
  "description": "A URL shortener with React frontend and serverless backend",
  "scripts": {
    "build": "cd frontend && npm run build",
    "dev": "cd frontend && npm run dev",
    "start": "cd frontend && npm run preview"
  },
  "dependencies": {
    "nanoid": "^5.1.5"
  }
}
